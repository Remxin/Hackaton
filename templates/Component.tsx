import React from 'react'

type ComponentProps = {
    id: string
    // other props
}

const Component = ({ }: ComponentProps) => {
    return (
        <div>Component</div>
    )
}

export default Component